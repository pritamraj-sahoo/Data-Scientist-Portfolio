// Placeholder for custom JavaScript if needed (e.g., form validation, animations)
document.getElementById('contact-form').addEventListener('submit', function(e) {
    e.preventDefault();
    alert('Your message has been sent!');
});
